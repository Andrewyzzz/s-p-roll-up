[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc = "0.8.19"
optimizer = true
optimizer_runs = 200

# Fork RPC endpoints — set via env vars
[rpc_endpoints]
base     = "${BASE_RPC}"
mainnet  = "${ETH_RPC}"
